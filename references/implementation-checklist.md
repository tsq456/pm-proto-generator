# 按本批范围验收

已确认上游要求优先，以下是未指定时的默认检查。只检查涉及的控件与流程。

## 来源与实现

- 本批来源、版本、授权依据可追溯，阻塞页未被误放行；HTML 与覆盖说明一致。
- page-id、Spec id、挂载参数与实际路径一致；资源来自包内。结构检查通过。
- 占位不提交、不补 CRUD；禁止批量的主表加 no-select；独立编码列不合并。

## 选型与复用

- 本批结构性实现有对应 implementation-map 条目，页型、宿主、layout 和区域组件与 Spec 一致。
- 复用位置可定位，实际行为满足要求；新增/适配有具体缺口和落点，不重复造已有组件。
- 全局脚本没有引入禁止的复选、操作或错误事件绑定；复杂能力未被擅自降为占位。
- 共享组件变化已回归本批受影响页面，记录实际验收结果与限制。

## 页面视觉

- 壳层一致，业务侧栏无 PRD 等非业务项；有导航变更才核对全部受影响入口。
- 列表筛选卡与表卡无重复标题；树层级可辨识；操作列对齐；固定单位放表头。
- 详情标题左侧返回，默认不附状态 Tag；描述表标签淡底、值区白，默认两列对；字段按上游顺序，未指定再按详情规范。通栏保持完整描边。
- 多 Tab 默认一体卡；详情内嵌表留边；局部编辑按钮在所在区域，默认当前页 Drawer 编辑。
- 字段视觉区分属性、分类、状态；表单默认双列，单位用前后缀。关闭按钮为图标且有可访问名称。
- 查询区默认点查询才生效；独立树搜索按业务说明，不套列表查询规则；数值范围用范围控件。
- 文案真实且符合业务；图片需要时从包内图片池选取，不造外链依赖。

## 交互与文档

- 每条本批核心流程至少走通一例，覆盖必要失败与边界反馈；跨页保持对象、上下文和返回关系。
- 有写操作才验增删改及刷新表现；已知 Mock 缺陷如实记录，不清空数据掩盖问题。
- 页面说明、更新记录、导航可读且抽屉互斥；完整包汇总可打开、可检索。
- Spec-only 不要求 HTML；增量不重验不相关全站。结果区分产品要求、原型覆盖与未决事项。
