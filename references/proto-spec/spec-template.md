# 页面规格说明模板（默认 · 扁平单文件）

默认每页只维护：

```text
proto-spec/<page-id>.md
```

文件名 = **page-id**（如 `tenant-list.md`），**不要**用中文文件名。YAML frontmatter = 页 meta；正文 = Screen Spec。

旧路径 `proto-spec/<page-id>/spec.md` 仍可读；新页不要再建子目录。  
总规则（谁驱动谁、产品 vs 演示）：[README.md](./README.md)。

---

## 骨架

```md
---
id: tenant-list
name: 租户列表
version: 0.1.0
status: draft
updatedAt: 2026-09-17
---

## 页面概述

1～3 句：本页解决什么、用户带着什么任务进来。可接「页面关系」。

## 功能范围

| 功能 | 界面入口 | 产品要求 | 原型覆盖 |
| --- | --- | --- | --- |
| … | … | … | 已演示 / 部分演示 / 未演示 |

## 角色与权限

| 角色 | 可见范围 | 操作权限 | 操作限制 |
| --- | --- | --- | --- |
| … | 本页可访问区域及数据范围 | … | … |

## 页面结构

| 页面分区 | 控件/内容 | 说明 |
| --- | --- | --- |
| … | … | … |

## 字段说明

### 表单字段

（本页无创建/编辑表单则整节省略）

| 名称 | 类型 | 必填条件 | 默认 | 校验/时机 | 错误提示 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| … | … | 创建时必填 / … | … | 失焦或提交时… | … | … |

### 筛选字段

（无筛选则省略）

| 名称 | 匹配方式 | 生效时机 | 重置结果 | 说明 |
| --- | --- | --- | --- | --- |
| … | 包含/精确/枚举… | 点「查询」 | 清空并恢复默认列表 | … |

### 展示字段

（列表列、详情只读等；无则省略）

详情页：表中**行序 = `.ob-desc` 展示序**，按 [detail-descriptions](../layouts/detail-descriptions.md)「字段摆放顺序」分层写（身份→状态→分类→归属→度量→审计→通栏收尾）。「字段同表单」只表示字段集合一致，不表示顺序一致。

| 名称 | 含义/口径 | 单位或格式 | 空值显示 | 说明 |
| --- | --- | --- | --- | --- |
| … | … | … | — / 「暂无」 | 数据来源一句 |

## 业务规则

| 规则 ID | 规则名 | 说明 |
| --- | --- | --- |
| BR-001 | … | 页内可写 BR-001；跨页引用用 `本页-id/BR-001` |

### 状态流转

（**有业务状态变化时必填**；纯查询页可整节省略。Mermaid 流程图可选。）

| 当前状态 | 操作 | 前置条件 | 目标状态 | 关联影响 |
| --- | --- | --- | --- | --- |
| 草稿 | 启用 | … | 已启用 | … |

## 交互说明

按控件/按钮写；关键操作最低完整度见 [interaction-template.md](./interaction-template.md)。

## 验收要点

（每条核心流程至少 1 条；描述可观察的预期结果，不以“与实现一致”代替要求。）

- 正常：…
- 异常/边界：…

## 待确认事项

> **阻塞确认**：列出会改变本批页面结构或核心行为、尚未解决的问题。无阻塞项时省略本提示。

| 问题 | 影响范围 | 是否阻塞本批实现 | 非阻塞时采用的假设 |
| --- | --- | --- | --- |
| … | 本页 / 跨页 | 是 / 否（说明理由） | … |

```

## Frontmatter 字段

| 键 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | = page-id = 文件名（无 `.md`） |
| `name` | 是 | 中文显示名 |
| `version` | 建议 | 如 `0.1.0` |
| `status` | 建议 | `draft` \| `reviewing` \| `confirmed` \| `deprecated` |
| `updatedAt` | 建议 | `YYYY-MM-DD` |

`confirmed` 须有与当前 Spec 版本相符的人工确认依据，记录在包内 `docs/spec-review.md`；不得自行改状态代替确认。见 [确认规则](./review-gate.md)。进入评审批次时 `version` / `status` 必填。

## 文风

**Screen Spec**：写清本页行为与口径；禁止空泛 PRD 套话。

- 页面名称由阅读器页头显示；正文从二级标题开始，章节内用三级标题。具体操作可直接作为交互说明下的三级标题，额外细节使用四级标题。
- 功能范围只概括功能、具体界面入口、产品行为与演示覆盖；交互条件、成功/失败和关联影响集中写在交互说明。
- 权限只在“角色与权限”集中维护，具体操作引用相关权限；未明确的数据可见范围须列为待确认，不自行推断。
- 用“新增弹窗”“列表工具栏”等具体区域名；禁止用“本页主区域”“页内可见”“正式对接 API”等无效占位替代业务说明。
- 字段、权限、规则适合表格；复杂交互逐操作分节，避免八列宽表。可选“原型演示说明”置于末尾，集中记录 ProtoMock 等演示机制。
- 阻塞项在“待确认事项”开头用引用块强调：`> **阻塞确认**：…`。无未决事项则省略该节；不能把未实现当作未确认。  
**产品要求**与**原型覆盖**分列；勿把「原型未做」写成「待产品确认」。

## 字段表怎么用

- 按本页实际用途拆表，**不必**三张都出现。
- 「必填」必须带场景（创建时 / 编辑时 / 筛选无必填等），禁止一张大表混用导致歧义。
- 详情「展示字段」按扫读分层排行，勿按表单填写序照抄；通栏（备注、附件）放表末。
- 旧包 `fields.yaml` 见 [fields-template.md](./fields-template.md)（仅兼容）。

## 挂载

```js
ProtoSpecRuntime.mount({
  specBase: '../proto-spec/',
  pageId: 'tenant-list',
  // …
});
```
