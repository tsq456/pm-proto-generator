# 上游接收改造验证记录

日期：2026-09-21。基线：16ed6d2。验证范围为技能协议、初始化和结构检查；未修改 Runtime、ProtoMock、既有示例或原型包。

## 自动检查

- `python3 -m unittest discover -s tests -v`：9 项通过。覆盖 Spec/实现阶段、错误退出码、本批筛选、未知页面、嵌套 sitemap、旧 meta + 分册、非法结构、重复 ID、错误路径、元信息不一致、planned、孤立 Spec、非空包补资源且逐文件字节不变、新包无业务示例及 --fix 不修改文件。
- 两份 SKILL.md 均通过 skill-creator 的 quick_validate；删除旧子技能 frontmatter 中不受该检查器支持的 disable-model-invocation 字段，不添加隐式调用限制。
- 主技能、README、references 和 skills 的 Markdown 本地链接全部存在；有效入口无退役流程引用。
- 主 SKILL.md 从 10,359 字符降至 4,900 字符，减少 52.7%。略超过原整理目标，不牺牲关键约束；完整视觉细则按任务加载。
- 既有 examples/proto-spec-demo 通过原型阶段检查。
- 既有 prototypes/style-review 如实报出 tenant-list、tenant-detail、backup-policy-form、cluster-overview、create-result 缺少 Spec；另警告 shell-multi-preview 未映射。未修改这些历史材料。

## 六类场景

| 场景 | 操作与结果 |
| --- | --- |
| 业务语言输入 | 使用 tests/fixtures/upstream-v1.md，正文无路由、英文枚举和技术 ID；派生 equipment-list/equipment-detail、字段键、normal 和 equipment-001。无需用户补技术字段。 |
| 多页与浮层 | 同一文档分为列表/详情两页；说明浮层归列表，基本信息/保管记录 Tab 归详情；共享对象统一，返回列表。没有为 Tab 或浮层增加页面。 |
| 冲突输入 | 审查下述空间资产样本；记录需检查、导航分歧和占位替换缺口，未自行选择导航或扩大实现范围。 |
| 明确例外 | 浏览器检查列表无复选框、编码独立列；说明仅展示/关闭；跨页及 Tab 可操作。 |
| 新版增量 | upstream-v2.md 只将详情保管人改为王芳；保留两页 ID 和第一版快照，未提及列表不删除。映射及来源集中记录。 |
| 阶段兼容 | 在创建 HTML/kits 之前通过 Spec 阶段检查；之后初始化保留现有文档，再通过原型阶段检查。自动测试另验证补回缺失 Runtime 文件且全部已有文件字节不变。 |

这是按新技能执行的人工转换演练，不是声称脚本能自动理解任意业务文档。脚本只验证结构，不证明业务授权或语义正确。

## 空间资产冲突审查

读取时间：2026-09-21；Notion 返回页面最后更新为 2026-09-20。

- [05.01 空间资产管理索引](https://app.notion.com/p/cc3d30ac70584b7a875bbda59a29ee2d)：版本 v0.1、状态“需检查”，要求核对固定独立菜单与全局“空间资产 + 租控视图”的差异。下游记录影响工作台、分层列表及公共导航，反馈 04 IA 与 05 模块负责人；不会用页面 confirmed 覆盖此状态。
- [模块正文](https://app.notion.com/p/dee67d328c5a4466b9edf5ae3142e18e) 第 10–11 节保留工作台及园区/地块/楼栋/房源独立页面和菜单。索引对全局 IA 的冲突提示仍需上游消除。本次没有读取整个项目，也没有假称已核验最新全局 IA 正文。
- [工作台 Spec](https://app.notion.com/p/bafdeffbe26b4a84a05500cb9d68049d) V0.1 的第 3、5、9–10 节限定详情和新建/编辑为占位，禁止表单提交；模块输入包又描述完整详情和编辑能力。二者可属于不同交付批次，不直接判为业务矛盾；在没有明确替换关系前，不自动将完整能力覆盖占位。由 06 页面 Spec 和 05 模块明确批次衔接。
- 工作台第 9.4 节明确不提供复选框，业务编码独立列；属于应保留的页面例外，不按默认表格规则改写。
- AS-P 与 PG 页面标识映射本身可由下游建立，不要求上游补技术键；只有页面业务含义、宿主或导航对应存在歧义时才反馈。
- 索引仍提示 Excel 导入范围冲突，而模块正文已明确不提供导入：记录来源不一致，核对上游版本与索引状态，不自行添加导入功能。

受影响内容仅可保留草稿；未证明独立闭环前不实施公共导航。此审查未写回 Notion，也未新增同步能力。

## 浏览器冒烟

临时包：`/tmp/pm-reception-smoke`（不纳入技能发布，不是实际业务原型）。通过本地静态服务器和隔离的无界面 Chrome 验证：

1. 列表显示固定会议屏；列序与无批量规则符合输入。
2. 打开说明并关闭；无提交。
3. 列表进入详情，保管记录显示第二版王芳，返回列表。
4. 原型说明加载对应 Markdown；导航目录含两页；根 PRD 汇总正常聚合。
5. 页面 JavaScript 无异常。

浏览器工具只用于本次验证，不成为技能运行依赖。运行库接口保持不变。

## 后续独立批次

ProtoMock 的删除记录复活、旧记录缺失字段补齐仍未修改。保留既定策略：删除留标记直到人工重置；补缺字段但保留已有值。本次不通过清空存储或迁移历史包规避问题。
